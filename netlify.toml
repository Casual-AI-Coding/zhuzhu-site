[build]
  command = "npm run build"
  publish = "dist"

# API 代理重定向 - 开发环境用 Vite 代理，生产环境用 Netlify 重定向
[[redirects]]
  from = "/notion-api/*"
  to = "https://api.notion.com/v1/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
