# 分享海报优化设计方案

> 版本: 1.0  
> 日期: 2026-02-21  
> 状态: 待实现

---

## 目录

1. [添加情侣照片](#1-添加情侣照片)
2. [里程碑特殊样式](#2-里程碑特殊样式)
3. [多模板选择](#3-多模板选择)

---

## 1. 添加情侣照片

### 设计目标

```
┌─────────────────────────────────────┐
│              ✨ zhuzhu ✨            │
│                                         │
│         ┌─────────────────┐           │
│         │                 │           │
│         │    情侣照片     │           │  ← 新增照片区域
│         │                 │           │
│         └─────────────────┘           │
│                                         │
│             我们在一起                 │
│                 519                   │
│                 天                     │
│                                         │
│           从 2024年9月1日 开始         │
│                                         │
│          💕 大萝卜 ❤️ 小葡萄         │
└─────────────────────────────────────┘
```

### 实现方案

**数据来源：**
- 使用首页轮播的照片列表
- 随机选择一张，或使用当前展示的照片

**代码实现：**

```javascript
// SharePoster.vue
const props = defineProps({
  photoUrl: { type: String, default: '' },
});

// 如果有照片，在 Canvas 中绘制
if (props.photoUrl) {
  const img = new Image();
  img.crossOrigin = 'anonymous';
  img.onload = () => {
    // 绘制照片（居中，圆角）
    ctx.save();
    ctx.beginPath();
    roundedRect(ctx, 150, 200, 300, 200, 16);
    ctx.clip();
    ctx.drawImage(img, 150, 200, 300, 200);
    ctx.restore();
  };
  img.src = props.photoUrl;
}

// 圆角矩形辅助函数
function roundedRect(ctx, x, y, width, height, radius) {
  ctx.beginPath();
  ctx.moveTo(x + radius, y);
  ctx.lineTo(x + width - radius, y);
  ctx.quadraticCurveTo(x + width, y, x + width, y + radius);
  ctx.lineTo(x + width, y + height - radius);
  ctx.quadraticCurveTo(x + width, y + height, x + width - radius, y + height);
  ctx.lineTo(x + radius, y + height);
  ctx.quadraticCurveTo(x, y + height, x, y + height - radius);
  ctx.lineTo(x, y + radius);
  ctx.quadraticCurveTo(x, y, x + radius, y);
  ctx.closePath();
}
```

---

## 2. 里程碑特殊样式

### 里程碑映射

| 天数 | 标签 | 样式 |
|------|------|------|
| 520 | 我爱你 | 粉色爱心主题 + "我爱你" |
| 1314 | 一生一世 | 红色主题 + "一生一世" |
| 365 | 一周年 | 蛋糕蜡烛 + "一周年" |
| 730 | 两周年 | 蛋糕蜡烛 × 2 |
| 1095 | 三周年 | 蛋糕蜡烛 × 3 |
| 666 | 666 | 金色 + "666" |
| 888 | 发发发 | 金色 + "888" |
| 999 | 久久久 | 金色 + "999" |

### 设计效果

**520天 - 我爱你：**
```
┌─────────────────────────────────────┐
│              💖 520 💖              │
│            我爱你                   │
│          ❤️💕❤️💕❤️                 │
└─────────────────────────────────────┘
```

**1314天 - 一生一世：**
```
┌─────────────────────────────────────┐
│            💕 1314 💕              │
│            一生一世                 │
│            💑→💏                   │
└─────────────────────────────────────┘
```

**周年纪念：**
```
┌─────────────────────────────────────┐
│            🎂 365 🎂               │
│            一周年                   │
│           🕯️   🕯️   🕯️            │
└─────────────────────────────────────┘
```

### 代码实现

```javascript
// 获取里程碑信息
function getMilestoneInfo(days) {
  const milestones = {
    520: { label: '我爱你', emoji: '💖', theme: 'love' },
    1314: { label: '一生一世', emoji: '💕', theme: 'forever' },
    365: { label: '一周年', emoji: '🎂', theme: 'anniversary' },
    730: { label: '两周年', emoji: '🎂', theme: 'anniversary' },
    1095: { label: '三周年', emoji: '🎂', theme: 'anniversary' },
    666: { label: '666', emoji: '✨', theme: 'lucky' },
    888: { label: '发发发', emoji: '✨', theme: 'lucky' },
    999: { label: '久久久', emoji: '✨', theme: 'lucky' },
  };
  
  return milestones[days] || null;
}

// 根据主题应用不同样式
function applyMilestoneStyle(ctx, theme, days, width) {
  switch(theme) {
    case 'love':
      ctx.fillStyle = '#FF6B9D';
      // 绘制粉色爱心装饰
      break;
    case 'forever':
      ctx.fillStyle = '#FF4757';
      // 绘制红色主题
      break;
    case 'anniversary':
      ctx.fillStyle = '#FFB347';
      // 绘制蛋糕蜡烛
      break;
    case 'lucky':
      ctx.fillStyle = '#FFD700';
      // 绘制金色主题
      break;
  }
}
```

---

## 3. 多模板选择

### 模板风格

| 模板 | ID | 背景渐变 | 装饰元素 | 字体风格 |
|------|-----|----------|----------|----------|
| 浪漫 | romantic | #FFF5F0 → #FFE4D6 | 心形、玫瑰 | 手写体 |
| 简约 | simple | #FAFAFA → #F5F5F5 | 极简线条 | 现代无衬线 |
| 可爱 | cute | #FFF8E7 → #FFE4E1 | 卡通、蝴蝶结 | 圆润字体 |
| 清新 | fresh | #F0F8FF → #E8F4F8 | 叶子、花朵 | 文艺风 |

### UI 设计

```
┌─────────────────────────────────────┐
│  选择海报风格                        │
├─────────────────────────────────────┤
│                                     │
│  ┌────────┐ ┌────────┐ ┌────────┐ │
│  │        │ │        │ │        │ │
│  │  🌸    │ │  🌙    │ │  💕    │ │
│  │ 浪漫  │ │ 简约  │ │ 可爱  │ │
│  │ 粉色  │ │ 白色  │ │ 奶油  │ │
│  └────────┘ └────────┘ └────────┘ │
│                                     │
│  ┌────────┐                        │
│  │        │                        │
│  │  🌊    │                        │
│  │ 清新   │                        │
│  │ 蓝色   │                        │
│  └────────┘                        │
│                                     │
│  当前选择: 浪漫                     │
└─────────────────────────────────────┘
```

### 模板配置

```javascript
const templates = {
  romantic: {
    name: '浪漫',
    gradient: ['#FFF5F0', '#FFE4D6'],
    decorations: ['💕', '💗', '💖', '🌹'],
    font: 'serif',
    accentColor: '#FF6B6B',
  },
  simple: {
    name: '简约',
    gradient: ['#FAFAFA', '#F5F5F5'],
    decorations: ['─', '│', '○'],
    font: 'sans-serif',
    accentColor: '#666666',
  },
  cute: {
    name: '可爱',
    gradient: ['#FFF8E7', '#FFE4E1'],
    decorations: ['🎀', '🌸', '⭐', '🎀'],
    font: 'cursive',
    accentColor: '#FFB6C1',
  },
  fresh: {
    name: '清新',
    gradient: ['#F0F8FF', '#E8F4F8'],
    decorations: ['🌿', '🌸', '🍃', '🌼'],
    font: 'serif',
    accentColor: '#87CEEB',
  },
};
```

### 模板选择器组件

```vue
<template>
  <div class="template-selector">
    <p class="text-sm text-text-secondary mb-2">选择海报风格</p>
    <div class="flex gap-2 flex-wrap">
      <button
        v-for="template in templates"
        :key="template.id"
        @click="$emit('select', template.id)"
        class="template-btn"
        :class="{ 'is-selected': selected === template.id }"
        :style="{ background: `linear-gradient(135deg, ${template.gradient[0]}, ${template.gradient[1]})` }"
      >
        <span class="template-icon">{{ template.icon }}</span>
        <span class="template-name">{{ template.name }}</span>
      </button>
    </div>
  </div>
</template>
```

---

## 验收标准

### 添加情侣照片
- [ ] 可显示首页轮播中的照片
- [ ] 照片居中展示，带圆角
- [ ] 照片加载失败时显示占位图

### 里程碑特殊样式
- [ ] 520天显示"我爱你"和粉色主题
- [ ] 1314天显示"一生一世"和红色主题
- [ ] 365/730/1095天显示蛋糕蜡烛
- [ ] 666/888/999天显示金色主题
- [ ] 其他天数显示普通样式

### 多模板选择
- [ ] 提供4种模板选择
- [ ] 模板切换实时生效
- [ ] 每种模板有独特的视觉风格
- [ ] 当前选中状态明确
